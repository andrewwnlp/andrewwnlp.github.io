title: Andrew Wang
description: CS PhD student at Johns Hopkins University

# Optional second form of the name, shown beside the masthead title
# (a name in another script, a nickname, the form you publish under).
# Leave empty to hide it entirely.
alias: ""
logo: /assets/img/pfp2.jpeg

url: https://andrewwnlp.github.io
repository: andrewwnlp/andrewwnlp.github.io

plugins:
  - jekyll-seo-tag

# Kept out of _site: local scratch, and files that are for the repo
# rather than the published site.
exclude:
  - temp
  - preview
  - Gemfile
  - Gemfile.lock
  - CLAUDE.md
  - README.md
